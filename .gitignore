# Python 字节码
__pycache__/
*.pyc

# 导出产物：每次导出都会重新生成，属于输出而非源
# （原始 PDF、标注、编辑数据都不在这里，见下方说明）
exports/

# 临时/探测脚本与它们的输出
_*.py
_*.out
_*.txt
_*/
*.bak
*.bak-*

# 字体下载包（90MB，只用于开发期转换，产物在 assets/fonts/）
*.zip

# ★ 测试的临时目录：所有测试的写入都落在这里，不进版本库。
#   这条是刻意的 —— 测试**绝不能**往用户的 documents/ exports/
#   edits/ annotations/ 里写东西（见 testscratch.py）。
_scratch/

# 字体转换用的 venv / pip 缓存都不在项目里（见 makefont.py），
# 但 assets/fonts/*.ttf 是**要提交的资产**（锁定的中文字体）。

# documents/：服务端默认扫描目录，放的是**用户自己的 PDF**，不是项目源码。
# 整个目录忽略 —— 本规则覆盖了原先只忽略测试夹具（documents/_*.pdf）的写法。
# 若将来确实要往仓库里放样例 PDF，用 !documents/样例.pdf 显式放行。
documents/

# 运行时产生的用户数据（与 exports/ 同类：属于输出，不属于源）
annotations/
edits/

# AI 助手的工作状态（记忆笔记、机械扫描原始产物），不属于项目源码
.workbuddy-ai/

# 编辑器/系统杂项
.DS_Store
Thumbs.db
